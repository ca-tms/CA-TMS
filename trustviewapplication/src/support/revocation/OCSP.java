// Standard path validation including all validity and revocation checking
// is done by Firefox and passed on to this application via the Firefox plugin.
// The application itself does not implement it currently.
